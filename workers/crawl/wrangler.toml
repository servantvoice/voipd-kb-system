#:schema node_modules/wrangler/config-schema.json
name = "cf-crawl"  # Change to your worker name
main = "src/index.ts"
compatibility_date = "2026-03-25"

[triggers]
crons = ["0 2 * * SUN"]

[observability]
enabled = true

[observability.logs]
enabled = true
invocation_logs = true

# No [vars] — use .dev.vars (local) or CF dashboard (production)
# Required vars: CF_ACCOUNT_ID
# Secrets: CF_API_TOKEN, CRAWL_SECRET, PIPELINE_URL

[[r2_buckets]]
binding = "KB_BUCKET"
bucket_name = "servant-voice-kb"  # Change to your R2 bucket name

[[workflows]]
binding = "CRAWL_WORKFLOW"
name = "cf-crawl-workflow"
class_name = "CrawlWorkflow"

# Optional: service binding to pipeline worker (uncomment after pipeline is deployed)
# [[services]]
# binding = "PIPELINE_WORKER"
# service = "cf-pipeline"
