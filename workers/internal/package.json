{
  "name": "@sv-kb/internal",
  "version": "1.0.0",
  "private": true,
  "description": "CF Worker serving the internal KB — all articles with admin overrides, behind CF Access",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
